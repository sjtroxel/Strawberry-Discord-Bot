# frozen_string_literal: true

source "https://rubygems.org"

ruby "3.4.4"

# Core Rails (API mode)
gem "rails", "~> 8.0.2"
gem "puma", "~> 6.4"

# Choose your database:
# If you're just testing locally, use sqlite3
gem "sqlite3", "~> 2.0"
# If you’ll deploy with Postgres, switch to:
# gem "pg", ">= 1.1"

# Speed up boot (caching)
gem "bootsnap", require: false

# HTTP requests to Utopia Intel site
gem "httparty"

# Optional: easy Discord webhooks
gem "discord-notifier"

# Background jobs
gem "sidekiq"

# Optional: scheduling background tasks
gem "whenever", require: false

group :development, :test do
  gem "pry"          # debugging
  gem "rspec-rails"  # testing framework (optional)
end

group :development do
  gem "listen", "~> 3.8"
  gem "spring"
  gem "spring-watcher-listen", "~> 2.1"
end